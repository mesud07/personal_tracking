import 'package:flutter/material.dart';

class LoginPageString{
  final snackBar = const  SnackBar(
          content: const Padding(
            padding: EdgeInsets.all(8.0),
            child: Text('Hesabı doğru girdiğinizden emin olunuz.'),
          ),
          
        );
}